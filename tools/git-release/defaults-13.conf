#!/bin/sh
#
# $FreeBSD$
#

. "${__BUILDCONFDIR}/main.conf"

BUILDTYPE="snap"

SRCBRANCH="main"
PORTSBRANCH="head"
DOCBRANCH="main"

WITH_DVD=
WITH_COMPRESSED_IMAGES=1
XZ_THREADS=10

case ${BUILDTYPE} in
	snap)
		NODOC=1
		;;
	*)
		NODOC=
		;;
esac

KERNEL_FLAGS=""
