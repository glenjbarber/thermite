#
# $FreeBSD$
#

o For a new build, run 'setrev.sh -b <branch>'.

o Run 'releasebuild.sh -c builds-{rev}.conf', which handles
  the builds from start to finish.

o When build is done, run 'ftp-stage.sh -c builds-{rev}.conf'
  to stage resulting images to hierarchy that matches ftp-master.
