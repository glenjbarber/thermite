#!/bin/sh
#
# $FreeBSD$
#

. "${__BUILDCONFDIR}/main.conf"

export BUILDSVNREV="HEAD"
export BUILDDATE="20140918"

releasesrc="stable/10"
heads=
stables="10"

revs="${heads} ${stables}"
archs="amd64 i386 ia64 powerpc powerpc64 sparc64 armv6"
types="snap"

x86_kernels="GENERIC"
armv6_kernels="BEAGLEBONE RPI-B PANDABOARD WANDBOARD-QUAD ZEDBOARD"
kernels="${x86_kernels} GENERIC64 ${armv6_kernels}"
