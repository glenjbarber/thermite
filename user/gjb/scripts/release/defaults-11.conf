#!/bin/sh
#
# $FreeBSD$
#

. "${__BUILDCONFDIR}/main.conf"

BUILDSVNREV="HEAD"
BUILDDATE="20140918"
BUILDTYPE="snap"

SRCBRANCH="base/head@${BUILDSVNREV}"
PORTBRANCH="ports/head@HEAD"
DOCBRANCH="doc/head@HEAD"

WITH_DVD=
WITH_COMPRESSED_IMAGES=1

case ${BUILDTYPE} in
	snap)
		NOPORTS=1
		NODOC=1
		;;
	*)
		NOPORTS=
		NODOC=
		;;
esac
